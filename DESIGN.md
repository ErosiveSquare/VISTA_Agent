# DESIGN.md — 设计决策记录

每条决策的格式是：**决策 → 备选方案 → 我为什么没选它 → 代价是什么**。

没有代价的决策不是决策，是常识。所以每一条都写了代价。

---

## 目录

- [D1 单线程 while 循环，不做多智能体](#d1-单线程-while-循环不做多智能体)
- [D2 九个专用工具，而不是只给一个 bash](#d2-九个专用工具而不是只给一个-bash)
- [D3 编辑格式选 str_replace](#d3-编辑格式选-str_replace)
- [D4 历史只追加，压缩靠插入标记](#d4-历史只追加压缩靠插入标记)
- [D5 按可重取性三分类压缩](#d5-按可重取性三分类压缩)
- [D6 reclaimable 由工具层静态决定](#d6-reclaimable-由工具层静态决定)
- [D7 压缩阈值 0.6，且只在 TODO 边界](#d7-压缩阈值-06且只在-todo-边界)
- [D8 KeyInfo 用固定 schema](#d8-keyinfo-用固定-schema)
- [D9 Verify-Gate 的基线对比与目标失败](#d9-verify-gate-的基线对比与目标失败)
- [D10 降级验收时诚实标注 verified=false](#d10-降级验收时诚实标注-verifiedfalse)
- [D11 RepoMap 自实现 PageRank](#d11-repomap-自实现-pagerank)
- [D12 技能卡用关键词+IDF，不用 embedding](#d12-技能卡用关键词idf不用-embedding)
- [D13 指纹守卫](#d13-指纹守卫)
- [D14 轻量文件快照，不用影子 git](#d14-轻量文件快照不用影子-git)
- [D15 不上容器隔离](#d15-不上容器隔离)
- [D16 零必需依赖](#d16-零必需依赖)
- [D17 默认 provider 用标准库直连](#d17-默认-provider-用标准库直连)
- [D18 role-based 多模型路由](#d18-role-based-多模型路由)
- [D19 内置离线演示](#d19-内置离线演示)
- [D20 评测判分不看 agent 自述](#d20-评测判分不看-agent-自述)
- [实现与设计规格书的差异说明](#实现与设计规格书的差异说明)

---

## D1 单线程 while 循环，不做多智能体

**决策**：`loop.py` 是一个朴素的 `for step in range(max_steps)` 循环，没有图编排、
没有子代理、没有异步队列。

**依据**：有源码级研究指出，Claude Code 里只有约 1.6% 的代码是 AI 决策逻辑，
其余 98.4% 是权限门、上下文管理、工具路由、恢复逻辑——loop 本身就是 while 循环。
另一个证据是 mini-swe-agent：100 行、只有一个 bash 工具，SWE-bench Verified 超过 74%。

**备选**：多智能体（planner / coder / reviewer 分工）。

**为什么没选**：多智能体在编程场景的收益证据不足，而调试成本与上下文碎片化成本
非常确定。在有限的开发预算下，我选择换深度而不是换架构复杂度。

**代价**：无法并行探索。如果要加，我会先加**只读的 explore 子代理**——
探索阶段产生的垃圾 token 最多，隔离收益最明确，且只读意味着没有写冲突。

---

## D2 九个专用工具，而不是只给一个 bash

**决策**：`read_file` / `write_file` / `edit_file` / `grep` / `repo_map` / `bash` /
`memory_read` / `memory_write` / `ask_user`，外加两个控制信号 `todo_write` / `finish`。

**备选**：只给 bash（mini-swe-agent 的做法）。

**为什么没选**（三个理由，第二个最重要）：

1. `edit_file` 走 str_replace 比让模型写 `sed` 可靠得多，`sed` 的转义地狱是真实失败源。
2. **专用工具让我能在工具层挂钩子**——指纹守卫、快照、`reclaimable` 标注都发生在
   这一层。如果一切都是 bash，我就失去了这些拦截点。
3. 结构化的工具结果是我压缩策略的前提，因为我需要知道哪些输出是可重取的。

**代价**：模型要学更大的动作空间。所以我控制在九个，没有超出
read / search / edit / execute / validate 这五个能力类别去发明工具。
而且消融表里有「裸基线」这一行，就是只给 bash 的对照。

---

## D3 编辑格式选 str_replace

**决策**：`edit_file(path, old_str, new_str, replace_all=False)`，要求唯一匹配。

**备选一**：整文件重写。token 成本随文件大小线性增长，且模型容易在无关处引入改动。

**备选二**：unified diff。依赖行号，模型算错行号的概率不低，上下文行不匹配就整块失败。

**为什么选它**：不依赖行号，**失败信息本身是可执行的**。匹配 0 次时我能告诉模型
「最接近的是这两处，相似度 0.87，在第 42 行附近」；匹配多次时我能告诉它
「匹配了 3 处，分别在第 12、47、93 行，请扩大 old_str」。这也是收敛结论——
多个开源 agent 独立选了这个格式。

**代价**：模型必须逐字符复制原文，包括缩进。为此做了两层容错：
自动去除误抄入的行号前缀（`strip_line_numbers`），以及归一化空白后的近似定位
（`find_near_matches`）。行号分隔符刻意用 `│` 而不是 `:`，就是为了让误抄可被识别。

---

## D4 历史只追加，压缩靠插入标记

**决策**（不变式 I1）：`History.events` 只追加。压缩时插入一条 `compaction` 事件，
给被覆盖的事件打 `superseded_by` 标记，原事件永远保留。发给模型的视图由
`view()` 计算得出。

**备选**：破坏性覆写——直接把旧消息列表替换成摘要。

**为什么没选**：一份数据要服务四个用途——发给模型的视图、L4 归档、`--resume` 恢复、
事后回放调试。破坏性覆写意味着要维护两套数据。

一个实现细节值得记一笔：压缩标记带一个排序键 `order`，取被覆盖区间起点减 0.5，
因此摘要出现在它所替代的**逻辑位置**，而不是被追加到最新事件之后。
最初的版本没有这个键，结果「旧工作的摘要」出现在新事件后面，语义完全颠倒。

**代价**：内存里保留全部事件。单次会话在几百事件量级、几 MB 文本，不是问题。
真要跑几千步，我会把老事件 spill 到磁盘，视图只读近期 + 压缩标记。

---

## D5 按可重取性三分类压缩

**决策**：PINNED 原样保留 / RECLAIMABLE 丢正文留锚点 / DERIVED 交弱模型压成 KeyInfo。

**备选**：对全部内容做统一的 LLM 摘要。

**为什么没选**：编程域有一个通用域没有的杠杆——**文件是可重取的**。
对可重取内容做有损摘要是没必要的信息损失。一个 500 行文件的正文占几千 token，
它的锚点（路径 + 行范围 + sha256 + 一行符号摘要）只占 20 个，
而重读的代价只是一次工具调用。

**代价**：模型需要偶尔重读文件。为了让这个机制真正生效，system prompt 里必须
明确写「看到锚点时如果需要具体内容请重新 read_file——这是设计如此」。
不说这句，模型看到锚点会困惑或者硬猜。

---

## D6 reclaimable 由工具层静态决定

**决策**：`reclaimable` 写死在工具注册表里，不由 LLM 判断。
`read_file` / `grep` / `repo_map` 恒为 true；bash 按只读命令白名单判定；其余为 false。

**为什么重要**：这让**压缩决策成为确定性、可复现的**。同一段历史压缩两次，
丢弃的内容完全相同。如果交给 LLM 判断，压缩就变成了一个不可复现的随机过程，
出问题时无法调试。

**代价**：判定必须保守。bash 的只读白名单要求命令头在白名单内、不含重定向、
不含 `-i` 之类的原地修改参数。不确定时一律返回 false——代价只是压缩效率略低，
不会丢信息。

---

## D7 压缩阈值 0.6，且只在 TODO 边界

**决策**：$\theta = 0.6$；且只在 TODO 项边界压缩，`max_overdue = 10` 是逃生阀。

**为什么不是 0.9**：压缩的目的不是防溢出，是**防 context rot**。等到 90% 才压缩时，
前面几十步的生成已经在被污染的上下文上进行了。

**为什么要 TODO 边界**：固定周期压缩不看内容，经常在一个子目标进行到一半时
把还需要的信息抹掉。

**为什么要逃生阀**：TODO 边界约束有一个漏洞——模型迟迟不完成 TODO 项时会永远不压缩，
最终真的溢出。`max_overdue` 步之后强制忽略边界。这个边界情况是实现时想到的，
写下来是因为它比主逻辑更能说明「这个设计被认真推敲过」。

**代价**：$\theta = 0.6$ 是经验值，**我没有做系统的参数扫描**。这是本项目的一个
已知不足，不应该假装它是调出来的。

---

## D8 KeyInfo 用固定 schema

**决策**：不可重取内容压缩成六个固定字段：`goal` / `verified_facts` / `rejected` /
`open_questions` / `touched_files` / `next_step`。

**备选**：让弱模型自由摘要。

**为什么没选**：有研究指出，自由摘要的输出量与保留内容在不同运行间波动很大，
prompt 里控制体积的指令基本被忽略。固定 schema 把「丢什么、留什么」变成可预测的。

`rejected`（已排除的方向及排除依据）这一项是刻意设计的——它直接对抗
「agent 反复尝试同一个已被否定的方案」这个已知失败模式。

**代价**：schema 之外的信息会丢。另外弱模型可能不遵守 JSON 格式，
所以有一条**确定性降级路径** `_heuristic_keyinfo`：不依赖任何模型，
从验收记录、工具错误、变更文件里抽取结构化事实。压缩永远不会因为模型故障而失败。

---

## D9 Verify-Gate 的基线对比与目标失败

**决策**：

$$\text{pass} \iff (F_1 \setminus F_0 = \varnothing) \;\wedge\; (T \cap F_1 = \varnothing) \;\wedge\; (\text{lint} = 0)$$

**第一项**（不引入新失败）解决的是：项目原本就有失败的测试，要求全绿不合理。

**第二项**（目标失败必须转绿）解决的是第一项自己制造的漏洞——修 bug 类任务里，
待修的失败本来就在 $F_0$ 中，只看第一项的话「什么都不改」也能平凡地通过。

$T$ 的判据是：某个既有失败的回溯里出现了本次被修改的文件，就认为它是本次任务的目标。
这个判据是启发式的，但它**可计算、可解释**：既不需要模型自我申报，
也不需要用户额外标注。

**代价**：判据可能误判。如果一个既有失败恰好也引用了被修改的文件，
但其实与本次任务无关，会被错误地要求修好。逃生开关是 `--verify-cmd` 手动指定
和 `--no-verify`。

还有一个真实的边界情况：**验收命令根本跑不起来**（项目声明了 pytest 但环境没装）。
这时如果按「退出码非零 = 失败」处理会永远过不了，按「解析不出失败 = 通过」处理
则 Verify-Gate 变成橡皮图章。实现里加了 `_not_runnable` 探测，
命中时降级到语法检查并标 `verified=false`。

---

## D10 降级验收时诚实标注 verified=false

**决策**：成功退出分两种——`success(verified=true)` 和 `success(verified=false)`，
CLI 输出、HTML 报告、L4 归档、评测 JSON 里都区分。

**为什么**：降级时的诚实标注比强行通过重要。「我知道我什么时候没验证」
本身就是一种可靠性。评测运行器的「谎报率」这一列，量化的正是这件事。

**代价**：用户看到 `verified=false` 时需要自己判断。这是有意的——
把不确定性显式暴露，好过把它藏起来。

---

## D11 RepoMap 自实现 PageRank

**决策**：二十行幂迭代，未引入 networkx。

$$r^{(t+1)} = (1-d)p + dMr^{(t)}, \quad d = 0.85$$

边权 $w_{uv} = \sum_{s \in R_u \cap D_v} c_u(s) / |D(s)|$。**除以 $|D(s)|$ 是为了压制
`get` / `run` / `handle` 这类烂大街的名字**——一个只在两个文件里出现的名字，
比一个到处都是的名字更有定位价值。

悬挂节点的质量按 $p$ 重新分配；收敛判据是 $L_1$ 范数小于 $10^{-6}$。

**与 Aider 的差异**：Aider 用两档个性化权重（会话提及的标识符 ×10、聊天内文件 ×50），
我只做一档（焦点文件 ×20）。两档权重的边际收益需要调参验证，在开发预算内验证不了，
不如做一档并说明。

**额外加了一条 Aider 没有的规则**：`per_file_cap`（每文件最多 10 个符号）。
没有它的时候，一个高分文件里的 `to_dict` / `render` 这类琐碎方法会把整张索引占满，
而索引的价值恰恰在于让模型看见「仓库里有哪些地方」。这是实现时从实际输出里
发现的问题。

**代价**：正则降级路径的签名精度不如 tree-sitter。但排序只依赖符号名与引用计数，
对 PageRank 的影响有限。

---

## D12 技能卡用关键词+IDF，不用 embedding

**决策**：

$$\text{score}(c) = \frac{\sum_{w \in T_c \cap Q} \mathrm{idf}(w)}{\sqrt{|T_c|}}
\cdot \lambda_{\text{scope}} \cdot \lambda_{\text{stats}}$$

中文用二元切分 + 英文按 `\w+` 分词。

**为什么不用 embedding**：

1. 需要额外的 embedding 模型调用与向量存储，成本不划算。
2. **关键词匹配的误触发是可解释、可调试的**——问「为什么这次没命中」，
   我能立刻打开卡片看 triggers；embedding 的失配没法向用户解释。
3. 技能卡数量在几十量级，精确率比召回率重要。

**记忆污染防护三层**：只蒸馏 Verify-Gate 通过的轨迹（来源可信）；
`fail_streak >= 2` 自动停用（失败降权）；**纯 YAML，用户随时能打开看见并删掉**。

第三层是最实在的——任何自动机制都可能失效，但「能看见能改」永远有效。
这也是选 YAML 而不是向量库的原因之一。

**代价**：同义表达会漏。`SKIP` 出口（让弱模型有权拒绝蒸馏）用来避免技能库
被无价值卡片污染。

---

## D13 指纹守卫

**决策**（不变式 I6）：`read_file` 记录文件全文 sha256，任何编辑前重算比对，
不一致则拒绝并要求重读。

**解决的问题**：这个机制不产生新能力，它消除一整类**静默错误**——
agent 基于陈旧内容做串替换，`old_str` 恰好仍能匹配，于是「改成功了但改错了地方」。
这种错误不会报错，要到跑测试甚至上线才暴露。

触发场景很常见：agent 自己跑了 `black`/`prettier`；bash 命令改了文件；
用户在另一个窗口编辑了同一个文件。

**代价**：多一次重读。HTML 报告里有「指纹拦截」徽章，用来量化这个机制拦下了多少次。

一个连带效果：REPL 的 `/undo` 回滚之后，磁盘内容与账本记录不一致，
所以回滚会**整体作废账本**，强制 agent 下次编辑前重新读取。
这不是额外写的逻辑，是 I6 的自然结果。

---

## D14 轻量文件快照，不用影子 git

**决策**：只复制「即将被修改的文件」到 `.vista/sessions/<sid>/snapshots/`。

**备选**：影子 git 仓库（Cline 的做法）。

**为什么没选**：用户工作区可能有未提交的改动，VISTA 不应该去碰用户的版本控制状态。

**代价**：不能做跨文件的原子回滚。对 bash 类命令，快照的是
`git status --porcelain` 里的已跟踪改动文件，上限 20 个——这是启发式的，
可能漏掉命令新建的文件。

---

## D15 不上容器隔离

**决策**：不引入 Docker。防护由三层构成：危险模式表直接 deny、
默认拒绝的权限策略、写前快照。

**为什么**：VISTA 是交互式本地工具，**用户在场就是安全边界**——Aider 也是这个选择。
做无人值守评测时应该加容器，那是不同的威胁模型。

**危险模式表里有一条值得单独提**：`git push --force`、`git rebase`、`filter-branch`、
`reset --hard origin/`。本次作业明确禁止改写已推送的提交历史，
**这条外部约束被内化成了 agent 的系统约束**。

**代价**：如果模型被诱导执行了白名单之外但仍然有害的命令，只能靠快照回滚。

---

## D16 零必需依赖

**决策**：`dependencies = []`。所有外部库都是可选增强，各有降级路径。

| 可选依赖 | 降级 |
|---|---|
| `tiktoken` | 中英混合启发式估算 |
| `tree_sitter_language_pack` | 正则符号抽取器 |
| `PyYAML` | 内置 YAML 子集解析器（`util/miniyaml.py`） |
| `openai` | 标准库 urllib 直连 |
| `rich` | 自写 ANSI 渲染（`cli/render.py`） |
| `networkx` | 自写幂迭代 |
| `ripgrep` | Python 正则遍历 |

**为什么**：这不是偷懒，而是一个**可核验的合规声明**——
「没有使用任何 agent 框架或 SDK」可以用一行 `pip show` 验证。
副作用是任何人 clone 下来就能跑，`vista demo` 不需要任何前置安装。

**代价**：多写了大约 600 行降级代码。`miniyaml` 是其中最有风险的一块——
它只支持本项目需要的 YAML 子集。实现时踩过一个真实的坑：
最初没支持行内流式序列 `[a, b]`，因为开发环境装了 PyYAML 所以一直没暴露，
直到写评测题目时才发现。

---

## D17 默认 provider 用标准库直连

**决策**：三个 provider——`http`（默认，标准库 urllib）、`openai`（官方 SDK）、
`mock`（离线脚本回放）。

**为什么默认是 http**：题目允许用厂商 SDK，但自己写 HTTP 层意味着完全掌控请求构造、
SSE 解析、错误分类，而且这部分代码本身就是「模型输出解析自行实现」的证据。

SSE 流式解析里有一个必须处理的细节：**工具调用的 `arguments` 是按 index 增量下发的**，
必须按 index 累积拼接，不能假设一次到位。

**错误分类**：429 / 5xx / 超时 → `RetryableError`（指数退避 $2^n + \text{jitter}$，≤3 次）；
401 / 400 → `FatalLLMError`（不重试）；上下文超限 → `ContextOverflow`
（交主循环强制压缩后重试，二次超限才终止）。

**代价**：如果某个网关有非标准行为，SDK 可能兼容得更好——所以保留了 `openai` 这个选项。

---

## D18 role-based 多模型路由

**决策**：`main` 负责推理与决策，`weak` 负责上下文压缩的 KeyInfo 生成与技能卡蒸馏。

**为什么可行**：weak 模型只承担「把已有信息重新组织」的任务，不承担决策，
所以能力要求低。Aider（main/weak/editor）与 OpenCode 都是这个思路。

**代价**：需要配两个模型。留空时 `weak` 自动复用 `main`，不会因此不可用。
HTML 报告里有 Model Routing 表，可以看到两个角色各自的调用次数与成本。

---

## D19 内置离线演示

**决策**：`vista demo` 用 `MockProvider` 回放预录脚本，在内置 fixture 上跑通完整循环。

**为什么值得花时间做**：

1. 任何人拿到仓库都能立刻看到系统在做什么，不必先配置模型
2. 演示视频可以用它录制，结果**完全确定、可复现**
3. 它本身就是一个端到端的冒烟测试

fixture 刻意只用标准库、验收命令用 `python3 -m unittest`，
因此在任何装了 Python 的机器上都能跑。fixture 里留了一个既有失败 `test_legacy`，
用来演示 Verify-Gate 的基线对比。

**代价**：脚本是预录的，不能演示模型的真实决策质量。这一点在 README 里写明了。

---

## D20 评测判分不看 agent 自述

**决策**：`run_eval.py` 跑完之后执行题目 YAML 里的独立验收命令，**只看退出码**。

**为什么**：与 Verify-Gate 同一个哲学——成败由环境裁定。而且验收命令由我编写，
不依赖 agent 生成的测试，否则判分是循环的（agent 可以写一个平凡通过的测试）。

结果表里额外有一列**谎报率**：agent 声称完成、但独立验收不通过的比例。
这一列正是 Verify-Gate 想压低的指标。

**代价**：验收命令要手写，题目扩展有成本。

---

## 实现与设计规格书的差异说明

规格书是开工前写的，实现过程中有几处调整。列出来是因为
「实现与设计不一致却不说明」比「调整了设计」更糟糕。

### 1. Event 用 `calls` 字段，而不是独立的 `tool_call` 事件

**规格书**：`EventKind` 里有 `tool_call` 一项。

**实现**：一条 `assistant` 事件携带 `calls: list[Call]`，工具结果是独立的
`tool_result` 事件。

**原因**：OpenAI 兼容接口本来就要求「一条 assistant 消息带 tool_calls 数组」，
拆成独立事件会在渲染视图时再合并回去，多一次转换没有收益。

### 2. Event 增加了 `order` 排序键

**规格书**：没有这个字段，视图按事件下标顺序渲染。

**实现**：`order` 默认等于下标；压缩标记取被覆盖区间起点减 0.5。

**原因**：这是实现时发现的真实缺陷。压缩标记追加在末尾，导致「旧工作的摘要」
出现在新事件之后，语义颠倒。用排序键解决，比在 `events` 列表中间插入更干净
（后者会破坏「下标即身份」）。

### 3. `compactable_span` 改为 `compactable`，返回事件列表

**规格书**：返回位置区间 `[a, b)`。

**实现**：返回待压缩的事件列表。

**原因**：引入 `order` 之后，「位置区间」不再等于「逻辑区间」，
直接返回事件列表消除了这个歧义。回合边界对齐（保证不变式 I2）也在这个函数里完成。

### 4. Verify-Gate 增加了「目标失败」判据

**规格书**：只有 $F_1 \setminus F_0 = \varnothing$。

**实现**：增加了 $T \cap F_1 = \varnothing$。

**原因**：见 D9。写 fixture 时发现「什么都不改也能通过」这个漏洞。

### 5. 增加了验收命令的可执行性探测

**规格书**：没有这一层。

**实现**：`_not_runnable(code, out)`，命中时降级到语法检查并标 `verified=false`。

**原因**：开发环境里没装 pytest，暴露了「验收命令跑不起来时 Verify-Gate 会变成
橡皮图章」这个问题。这是一个普遍情况，不只是开发环境的特殊性。

### 6. 规格书里的 `tools/interact.py` 与 `memtool.py` 合并了

`ask_user`、`memory_read`、`memory_write` 都在 `tools/memtool.py` 里，
控制信号 `todo_write` / `finish` 单独放在 `tools/control.py`。
按「是否与外部环境交互」分文件比按「是否是记忆」分更自然。

### 7. 没有引入 rich

**规格书**：依赖白名单里有 `rich`。

**实现**：自写 `cli/render.py`（ANSI 转义）。

**原因**：既然已经决定零必需依赖（D16），rich 提供的能力（颜色、流式输出、
简单框线）用几十行就够了，不值得为它破例。

实现时踩了一个坑：`Console` 类同时有 `stream` 属性（输出流）和 `stream()` 方法
（流式增量回调），实例属性把方法覆盖了，导致流式输出直接崩。属性改名为 `out` 解决。

### 8. 增加了 `vista demo` 与 `vista doctor`

规格书里没有这两个命令。`demo` 的理由见 D19；`doctor` 是因为零必需依赖设计
带来了「哪些降级路径正在生效」这个新问题，需要一个地方能一眼看清。
